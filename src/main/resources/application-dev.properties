# ── R2DBC (reactive) — transaction pooler port 6543 ──────────
spring.r2dbc.url=r2dbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslMode=require
spring.r2dbc.username=postgres.klvnzbprkkzfrewsvklb
spring.r2dbc.password=@PostAdmin?12

# ── Liquibase (JDBC) — session pooler port 5432 ──────────────
spring.liquibase.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require&user=postgres.klvnzbprkkzfrewsvklb
spring.liquibase.user=postgres.klvnzbprkkzfrewsvklb
spring.liquibase.password=@PostAdmin?12
spring.liquibase.change-log=classpath:db-migration/db-changelog-main.yaml

# ── Logging ──────────────────────────────────────────────────
logging.level.org.springframework.data.r2dbc=DEBUG

# .env file import
spring.config.import=optional:file:.env[.properties]

# jwt
jwt.secret=${jwt.secret}
jwt.expiration=${jwt.expiration}

